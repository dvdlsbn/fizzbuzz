<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.fizzbuzzletmecounttothatnumber"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:name=".FizzBuzzApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FizzBuzzLetMeCountThatNumber"
        tools:targetApi="31">

        <activity
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan"
            android:name="com.example.fizzbuzzletmecounttothatnumber.ui.activity.MainActivity"
            android:theme="@style/Theme.FizzBuzzLetMeCountThatNumber.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>